# HERMES TTS Configuration Example
# 
# This file shows configuration options for the TTS (Text-to-Speech) module.
# Copy to .env or configure via environment variables.

# ===== TTS Backend Selection =====
# Primary TTS backend to use
# Options: 'kokoro', 'openai', 'existing'
TTS_BACKEND=kokoro

# Fallback backend if primary fails
# Optional: 'kokoro', 'openai', 'existing'
TTS_FALLBACK_BACKEND=existing

# ===== Kokoro Configuration =====
# Kokoro API Mode (when using hosted Kokoro service)
# KOKORO_API_URL=https://api.kokoro-tts.example.com
# KOKORO_API_KEY=your-api-key-here

# Kokoro Local Mode (when running Kokoro locally)
# KOKORO_MODEL_PATH=/path/to/kokoro/models
# KOKORO_DEVICE=cuda  # or 'cpu'
# KOKORO_CACHE_DIR=./cache/kokoro

# ===== Voice Configuration =====
# Default voice for English
TTS_DEFAULT_VOICE_EN=kokoro-en-us-female-1

# Default voice for Spanish  
TTS_DEFAULT_VOICE_ES=kokoro-es-es-female-1

# Speech rate (1.0 = normal, 0.5 = half speed, 2.0 = double speed)
TTS_DEFAULT_SPEED=1.0

# ===== Performance Configuration =====
# Maximum synthesis latency in milliseconds (target)
TTS_MAX_LATENCY_MS=500

# Enable latency warnings for slow synthesis
TTS_WARN_ON_HIGH_LATENCY=true

# Enable synthesis caching for repeated text
TTS_ENABLE_CACHING=true
TTS_CACHE_SIZE=100

# ===== OpenAI Configuration (if using OpenAI backend) =====
# OpenAI API key (required for OpenAI backend)
# OPENAI_API_KEY=sk-your-openai-api-key

# OpenAI TTS model
# OPENAI_TTS_MODEL=tts-1-hd

# OpenAI default voice
# OPENAI_TTS_VOICE=nova
