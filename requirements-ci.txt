# HERMES CI/CD Dependencies
# This file contains the minimal dependencies needed for continuous integration and testing
# Note: Only include essential packages to avoid version conflicts

# Testing framework and coverage (core essentials)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
coverage[toml]==7.4.4

# Code quality and formatting (minimal set)
black==23.11.0
flake8==6.1.0
isort==5.12.0

# Essential production dependencies only (minimal subset)
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0
httpx>=0.24.0
requests>=2.28.0

# Type checking (optional - only if needed)
# mypy==1.7.1
# types-requests==2.31.0.10

# Security scanning (lightweight)
bandit==1.7.5

# Build tools (minimal)
wheel
setuptools