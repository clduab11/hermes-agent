name: 📊 Site Monitoring & Alerts

on:
  schedule:
    - cron: '*/30 * * * *'  # Every 30 minutes
    - cron: '0 9 * * 1-5'   # Business hours reminder (9 AM weekdays)
  workflow_dispatch:
    inputs:
      monitoring_type:
        description: 'Type of monitoring check'
        required: true
        default: 'full'
        type: choice
        options:
          - full
          - uptime
          - performance
          - legal-compliance

jobs:
  # Site uptime monitoring
  uptime-monitoring:
    name: 🌐 Site Uptime Monitoring
    runs-on: ubuntu-latest
    if: github.event.inputs.monitoring_type == 'uptime' || github.event.inputs.monitoring_type == 'full' || github.event.inputs.monitoring_type == '' || github.event_name == 'schedule'
    
    steps:
      - name: Check site availability
        run: |
          SITE_URL="https://clduab11.github.io/Hermes-beta/"
          echo "🔍 Checking HERMES demo site availability..."
          echo "📍 URL: $SITE_URL"
          
          # Multi-point availability check
          ATTEMPTS=3
          SUCCESS_COUNT=0
          
          for i in $(seq 1 $ATTEMPTS); do
            echo "🔄 Attempt $i/$ATTEMPTS"
            HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" --max-time 10 "$SITE_URL")
            RESPONSE_TIME=$(curl -o /dev/null -s -w "%{time_total}" --max-time 10 "$SITE_URL")
            
            echo "  📊 HTTP Status: $HTTP_STATUS"
            echo "  ⏱️  Response Time: ${RESPONSE_TIME}s"
            
            if [ "$HTTP_STATUS" -eq 200 ]; then
              ((SUCCESS_COUNT++))
              echo "  ✅ Success"
            else
              echo "  ❌ Failed (Status: $HTTP_STATUS)"
            fi
            
            [ $i -lt $ATTEMPTS ] && sleep 5
          done
          
          echo ""
          echo "📊 Results: $SUCCESS_COUNT/$ATTEMPTS successful"
          
          if [ $SUCCESS_COUNT -eq $ATTEMPTS ]; then
            echo "🎉 Site is fully operational"
            echo "uptime_status=UP" >> $GITHUB_ENV
          elif [ $SUCCESS_COUNT -gt 0 ]; then
            echo "⚠️ Site has intermittent issues"
            echo "uptime_status=DEGRADED" >> $GITHUB_ENV
          else
            echo "🚨 Site is DOWN"
            echo "uptime_status=DOWN" >> $GITHUB_ENV
            exit 1
          fi

      - name: Professional readiness check
        run: |
          if [ "$uptime_status" = "UP" ]; then
            echo "✅ HERMES demo site ready for professional showcase"
            echo "💼 Site available for LinkedIn demonstrations"
            echo "⚖️ Ready to present to legal industry professionals"
          else
            echo "⚠️ Site issues may impact professional presentations"
          fi

  # Performance monitoring
  performance-monitoring:
    name: ⚡ Performance Monitoring
    runs-on: ubuntu-latest
    if: github.event.inputs.monitoring_type == 'performance' || github.event.inputs.monitoring_type == 'full' || github.event_name == 'schedule'
    
    steps:
      - name: Performance metrics collection
        run: |
          SITE_URL="https://clduab11.github.io/Hermes-beta/"
          echo "⚡ Collecting performance metrics for HERMES demo..."
          
          # Collect detailed timing metrics
          METRICS=$(curl -o /dev/null -s -w "DNS:%{time_namelookup}\nConnect:%{time_connect}\nTTFB:%{time_starttransfer}\nTotal:%{time_total}\nSize:%{size_download}\nSpeed:%{speed_download}\n" "$SITE_URL")
          
          echo "$METRICS"
          
          # Extract key metrics
          DNS_TIME=$(echo "$METRICS" | grep "DNS:" | cut -d: -f2)
          CONNECT_TIME=$(echo "$METRICS" | grep "Connect:" | cut -d: -f2)
          TTFB=$(echo "$METRICS" | grep "TTFB:" | cut -d: -f2)
          TOTAL_TIME=$(echo "$METRICS" | grep "Total:" | cut -d: -f2)
          SIZE=$(echo "$METRICS" | grep "Size:" | cut -d: -f2)
          
          echo ""
          echo "📊 Performance Analysis:"
          echo "🔍 DNS Resolution: ${DNS_TIME}s"
          echo "🔗 Connection Time: ${CONNECT_TIME}s"
          echo "📡 Time to First Byte: ${TTFB}s"
          echo "⏱️  Total Load Time: ${TOTAL_TIME}s"
          echo "📦 Page Size: ${SIZE} bytes"
          
          # Performance thresholds for professional presentation
          if (( $(echo "$TOTAL_TIME < 2.0" | bc -l) )); then
            echo "✅ Excellent load time - professional ready"
            echo "performance_status=EXCELLENT" >> $GITHUB_ENV
          elif (( $(echo "$TOTAL_TIME < 5.0" | bc -l) )); then
            echo "✅ Good load time - suitable for demos"
            echo "performance_status=GOOD" >> $GITHUB_ENV
          else
            echo "⚠️ Slow load time - may impact professional impression"
            echo "performance_status=SLOW" >> $GITHUB_ENV
          fi

  # Legal compliance monitoring
  legal-compliance-monitoring:
    name: ⚖️ Legal Compliance Monitoring
    runs-on: ubuntu-latest
    if: github.event.inputs.monitoring_type == 'legal-compliance' || github.event.inputs.monitoring_type == 'full' || github.event_name == 'schedule'
    
    steps:
      - name: Legal compliance validation
        run: |
          SITE_URL="https://clduab11.github.io/Hermes-beta/"
          echo "⚖️ Monitoring legal compliance for professional legal tech demo..."
          
          # Fetch site content
          SITE_CONTENT=$(curl -s "$SITE_URL" || echo "Failed to fetch content")
          
          # Check for legal industry positioning
          COMPLIANCE_SCORE=0
          MAX_SCORE=10
          
          echo "🔍 Legal Industry Positioning Check:"
          
          if echo "$SITE_CONTENT" | grep -i "hermes\|legal.*tech\|ai.*voice"; then
            echo "✅ HERMES legal tech branding present"
            ((COMPLIANCE_SCORE++))
          fi
          
          if echo "$SITE_CONTENT" | grep -i "law.*firm\|legal.*professional"; then
            echo "✅ Law firm targeting evident"
            ((COMPLIANCE_SCORE++))
          fi
          
          if echo "$SITE_CONTENT" | grep -i "compliance\|hipaa\|gdpr"; then
            echo "✅ Compliance messaging present"
            ((COMPLIANCE_SCORE++))
          fi
          
          if echo "$SITE_CONTENT" | grep -i "cost.*sav\|efficienc\|automat"; then
            echo "✅ Value proposition messaging present"
            ((COMPLIANCE_SCORE++))
          fi
          
          if echo "$SITE_CONTENT" | grep -i "24.*7\|voice.*agent\|ai.*assistant"; then
            echo "✅ Key features highlighted"
            ((COMPLIANCE_SCORE++))
          fi
          
          echo ""
          echo "📊 Legal Compliance Score: $COMPLIANCE_SCORE/$MAX_SCORE"
          
          if [ $COMPLIANCE_SCORE -ge 8 ]; then
            echo "🏆 Excellent legal industry compliance"
            echo "💼 Ready for LinkedIn professional showcase"
          elif [ $COMPLIANCE_SCORE -ge 5 ]; then
            echo "✅ Good legal industry compliance"
            echo "💼 Suitable for professional demonstration"
          else
            echo "⚠️ Legal industry compliance needs improvement"
          fi

  # Business hours reminder
  business-readiness-alert:
    name: 💼 Business Readiness Alert
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' && github.event.schedule == '0 9 * * 1-5'
    
    steps:
      - name: Business readiness check
        run: |
          SITE_URL="https://clduab11.github.io/Hermes-beta/"
          echo "💼 HERMES Business Readiness Report - $(date '+%A, %B %d, %Y')"
          echo "=========================================================="
          
          # Quick health check
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" --max-time 10 "$SITE_URL" || echo "TIMEOUT")
          RESPONSE_TIME=$(curl -o /dev/null -s -w "%{time_total}" --max-time 10 "$SITE_URL" 2>/dev/null || echo "N/A")
          
          echo "🌐 Site Status: $HTTP_STATUS"
          echo "⚡ Response Time: ${RESPONSE_TIME}s"
          echo "📍 Demo URL: $SITE_URL"
          echo ""
          
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "✅ HERMES demo site is OPERATIONAL"
            echo "💼 Ready for professional presentations"
            echo "📱 LinkedIn showcase ready"
            echo "⚖️ Legal industry demonstration prepared"
            echo ""
            echo "🎯 Key Talking Points for Legal Professionals:"
            echo "   • 24/7 AI voice reception for law firms"
            echo "   • Significant cost savings vs. human staff"
            echo "   • HIPAA/GDPR compliant legal tech solution"
            echo "   • Reduces need for overseas outsourcing"
            echo "   • Professional client communication automation"
          else
            echo "🚨 SITE ISSUE DETECTED"
            echo "❌ Demo site may not be accessible for presentations"
            echo "🔧 Immediate attention required"
          fi
          
          echo ""
          echo "📊 Professional Readiness: $([ "$HTTP_STATUS" = "200" ] && echo "READY ✅" || echo "NEEDS ATTENTION ⚠️")"

  # Generate monitoring summary
  monitoring-summary:
    name: 📋 Monitoring Summary
    runs-on: ubuntu-latest
    needs: [uptime-monitoring, performance-monitoring, legal-compliance-monitoring]
    if: always() && (github.event.inputs.monitoring_type == 'full' || github.event.inputs.monitoring_type == '' || github.event_name == 'schedule')
    
    steps:
      - name: Aggregate monitoring results
        run: |
          echo "📊 HERMES Monitoring Summary - $(date)"
          echo "====================================="
          echo ""
          echo "🌐 Demo Site: https://clduab11.github.io/Hermes-beta/"
          echo "🎯 Purpose: Professional legal tech demonstration"
          echo "💼 Target: Law firm decision-makers and LinkedIn showcase"
          echo ""
          
          # Note: In a real scenario, you'd access the results from previous jobs
          # For now, we'll do a final verification
          SITE_URL="https://clduab11.github.io/Hermes-beta/"
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" --max-time 10 "$SITE_URL" 2>/dev/null || echo "ERROR")
          
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "✅ Overall Status: OPERATIONAL"
            echo "🎉 Professional demonstration ready"
            echo "⚖️ Legal industry compliance validated"
            echo "💰 Cost-saving value proposition showcased"
          else
            echo "⚠️ Overall Status: NEEDS ATTENTION"
            echo "🔧 Professional presentation may be impacted"
          fi
          
          echo ""
          echo "🎯 Next Steps:"
          echo "1. Verify all legal disclaimers are prominent"
          echo "2. Ensure value proposition is clear for law firms"
          echo "3. Validate cost-saving messaging effectiveness"
          echo "4. Confirm professional presentation readiness"

# This workflow provides continuous monitoring to ensure the HERMES demo site
# remains professional and ready for legal industry demonstrations.